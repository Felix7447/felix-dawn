<div class="logo-list-section">
  <div class="logo-list-container page-width">
    {% for item in section.blocks %}
      <div class="logo-list-item">
        {% if item.settings.logo %}
          <img src="{{ item.settings.logo | img_url: 'master' }}" alt="{{ item.settings.logo.alt }}">
        {% endif %}
        <h3>{{ item.settings.title }}</h3>
        <p>{{ item.settings.text }}</p>
        <a href="{{ item.settings.link }}">{{ item.settings.link_text }}</a>
      </div>
    {% endfor %}

  </div>
</div>

{% schema %}
  {
    "name": "logo-list-section",
    "settings": [
      {
        "type": "header",
        "content": "Container settings"
      }, {
        "type": "select",
        "id": "justify_content",
        "label": "Justify Content",
        "options": [
          {
            "label": "around",
            "value": "around"
          }, {
            "label": "between",
            "value": "between"
          }
        ]
      }
    ],
    "blocks": [
      {
        "name": "logo-list-item",
        "type": "block",
        "settings": [
          {
            "type": "image_picker",
            "id": "logo",
            "label": "Logo"
          },
          {
            "type": "text",
            "id": "title",
            "label": "Title"
          },
          {
            "type": "text",
            "id": "text",
            "label": "Text"
          },
          {
            "type": "url",
            "id": "link",
            "label": "Link"
          }, {
            "type": "text",
            "id": "link_text",
            "label": "Link Text"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Logo List section"
      }
    ]
  }
{% endschema %}