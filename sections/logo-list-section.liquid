<div class="logo-list-section">
  <div class="logo-list-container page-width">
    {% for item in section.blocks.settings %}
      <div class="logo-list-item">
        <img src="{{ item.logo | img_url: 'master' }}" alt="{{ item.logo.alt }}">
        <h3>{{ item.title }}</h3>
        <p>{{ item.text }}</p>
        <span>{{ item.link }}</span>
      </div>
    {% endfor %}

  </div>
</div>

{% schema %}
  {
    "name": "logo-list-section",
    "blocks": [
      {
        "name": "logo-list-item",
        "type": "block",
        "settings": [
          {
            "type": "image_picker",
            "id": "logo",
            "label": "Logo"
          }, {
            "type": "text",
            "id": "title",
            "label": "Title"
          }, {
            "type": "text",
            "id": "text",
            "label": "Text"
          }, {
            "type": "url",
            "id": "link",
            "label": "Link"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Logo List section"
      }
    ]
  }
{% endschema %}